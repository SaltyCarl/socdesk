__pycache__/
*.pyc
.venv/
site/data/
.pytest_cache/
site-tests/node_modules/
site-tests/test-results/
site-tests/playwright-report/
test-results/

# Local working conventions, deliberately not published: this repo is public
# and the file is an assistant-tooling convention, not project documentation.
# Everything a contributor needs is in README.md and docs/.
CLAUDE.md

# Local tooling/cache dirs — never part of the project
.claude/
.wrangler/
.playwright-mcp/

# SDD scratch (ledger, briefs, review packages)
.superpowers/
