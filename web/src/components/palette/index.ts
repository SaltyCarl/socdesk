/**
 * Command-palette barrel — component + types only (keeps this file
 * react-refresh-clean). Import the hook (`useCommandPalette`) and the
 * command helpers directly from their modules.
 */
export { CommandPalette } from './CommandPalette'
export type { CommandPaletteProps } from './CommandPalette'
export type { CommandItem, CommandGroup, CommandKind, IndicatorType } from './types'
