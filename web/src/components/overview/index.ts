/** Situational-board barrel — the start-of-shift board that fills the Overview
 *  below the globe hero. Only the composed board is public; the individual
 *  panels stay internal to this folder. */
export { SituationalBoard } from './SituationalBoard'
