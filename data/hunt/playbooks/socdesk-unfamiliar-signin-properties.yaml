# Drop-in replacement for data/hunt/playbooks/socdesk-unfamiliar-signin-properties.yaml
#
# WHAT CHANGED vs the shipped 2-step version
#   The shipped playbook is a *detection*: scope the IP, then score novelty —
#   two ways to confirm the same fact. This is an *investigation ladder*: each
#   step asks a different question, against a different table, in the order an
#   analyst actually works an account-compromise alert:
#       confirm -> scope -> persistence (rules / OAuth / device+MFA) -> token replay
#   Per-step guidance rides as `// Look for:` / `// Next:` KQL comments because
#   the published step schema is additionalProperties:false (id, title, kind,
#   param, dialect, tables, kql) and the loader strips anything else. Comments
#   are valid KQL, survive Copy, and pass the Kustainer gate.
#
# HARDENING (the gap from the 2026-09-04 review)
#   Every optional/dynamic column is wrapped in column_ifexists(...) so a
#   workspace that does not populate LocationDetails / AutonomousSystemNumber /
#   DeviceDetail / MfaDetail returns nulls instead of throwing. "Syntax-valid in
#   the emulator" is not "runs in the customer's workspace" — this closes that.
#
# TABLES (the defect CARL shipped — do not repeat it)
#   Inbox rules are Exchange operations and live in OfficeActivity, NOT AuditLogs.
#   AuditLogs carries the Entra ID directory audit (consent grants, device and
#   auth-method registration). Both DDLs already exist under
#   data/hunt/kusto_ddl/log_analytics/, so CI can validate all six steps today.
#
# INJECTION
#   Step 1 is param: ip — auto-filled from the enriched indicator.
#   Steps 2-6 are param: upn — {{upn}} stays visible and the panel's existing
#   "replace with a value an earlier step surfaced — never guessed" callout
#   applies. That is the correct behaviour: the account comes from step 1.
#
# METHODOLOGY PROVENANCE (public, non-employer)
#   The sequence is the standard account-compromise IR motion published by CISA
#   (#StopRansomware / cloud IR guidance) and Microsoft's own Entra ID
#   compromised-account response: validate the sign-in, then hunt persistence
#   (mail rules, OAuth grants, device/MFA registration), then check token reuse.
#   Queries below are original SOCDesk KQL written against the public Microsoft
#   table reference — no third-party or internal-tool content.

id: unfamiliar-signin-properties
title: Unfamiliar sign-in properties
alert_sources: [Entra ID Protection]
ioc_types: [ipv4, ipv6]
techniques: [T1078.004, T1621, T1114.003, T1098.005, T1550.001]
tested: "2026-09-04"

rationale: >
  Investigation ladder, not a detector. Step 1 decides whether the sign-in is
  actually suspicious (MFA satisfied? CA applied? managed device?) before an
  analyst escalates — most "unfamiliar sign-in" alerts die here, correctly.
  Steps 3-5 are the three persistence mechanisms an attacker establishes in the
  first minutes after a successful identity compromise, in the order they are
  most commonly seen. Step 6 catches the case where the credential was never
  phished at all and a stolen token is being replayed. Every step states what a
  hit means and where it sends the analyst next, so the playbook terminates in a
  decision rather than in a list of rows.

steps:
  - id: signin-context
    title: 1. Is this really compromise? Sign-in context for this IP
    kind: pivot
    param: ip
    dialect: log_analytics
    tables: [SigninLogs]
    kql: |
      // PURPOSE: validate before you escalate. An unfamiliar location with MFA
      // satisfied on a managed device is usually travel or a new ISP, not theft.
      // This step also surfaces the account(s) to carry into steps 2-6.
      SigninLogs
      | where TimeGenerated > ago(7d)
      | where IPAddress == "{{ip}}"
      | extend Country     = tostring(column_ifexists("LocationDetails", dynamic(null)).countryOrRegion)
      | extend City        = tostring(column_ifexists("LocationDetails", dynamic(null)).city)
      | extend OS          = tostring(column_ifexists("DeviceDetail", dynamic(null)).operatingSystem)
      | extend Managed     = tostring(column_ifexists("DeviceDetail", dynamic(null)).isManaged)
      | extend Compliant   = tostring(column_ifexists("DeviceDetail", dynamic(null)).isCompliant)
      | extend MfaMethod   = tostring(column_ifexists("MfaDetail", dynamic(null)).authMethod)
      | extend ASN         = tostring(column_ifexists("AutonomousSystemNumber", ""))
      | extend CAStatus    = tostring(column_ifexists("ConditionalAccessStatus", ""))
      | extend Risk        = tostring(column_ifexists("RiskLevelDuringSignIn", ""))
      | extend Outcome     = iff(ResultType == "0", "Success", strcat("Fail ", ResultType))
      | summarize Signins      = count(),
                  Successes    = countif(ResultType == "0"),
                  Failures     = countif(ResultType != "0"),
                  FirstSeen    = min(TimeGenerated),
                  LastSeen     = max(TimeGenerated),
                  Countries    = make_set(Country, 10),
                  Cities       = make_set(City, 10),
                  Apps         = make_set(AppDisplayName, 15),
                  MfaMethods   = make_set(MfaMethod, 10),
                  CAStatuses   = make_set(CAStatus, 10),
                  RiskLevels   = make_set(Risk, 10),
                  DeviceOS     = make_set(OS, 10),
                  ManagedFlags = make_set(Managed, 5),
                  Outcomes     = make_set(Outcome, 10)
          by UserPrincipalName, ASN
      | order by Successes desc, Signins desc
      //
      // Look for: a SUCCESS where MfaMethods is empty or "Password", CAStatuses
      //   is "notApplied", ManagedFlags has no "true", or RiskLevels is
      //   medium/high. That combination is the compromise signal.
      // Benign twin: MFA satisfied + managed/compliant device + a plausible
      //   travel country = new ISP or roaming. Close it.
      // Next: take UserPrincipalName from the top SUCCESS row into {{upn}} for
      //   steps 2-6. If there were no successes at all, this is spray/stuffing
      //   against you — pivot to the password-spray playbook instead.

  - id: novelty-baseline
    title: 2. Is this account's behaviour actually new? (trust-reduced novelty)
    kind: scenario
    param: upn
    dialect: log_analytics
    tables: [SigninLogs]
    kql: |
      // PURPOSE: separate "never seen before" from "unusual but known". Scores
      // this account's last 24h against its own 14-day baseline; a managed
      // device reduces the score because it is evidence of legitimacy.
      let lookback = 14d;
      let recent   = 1d;
      let baseline =
          SigninLogs
          | where TimeGenerated between (ago(lookback) .. ago(recent))
          | where UserPrincipalName =~ "{{upn}}"
          | where ResultType == "0"
          | extend Country = tostring(column_ifexists("LocationDetails", dynamic(null)).countryOrRegion)
          | extend OS      = tostring(column_ifexists("DeviceDetail", dynamic(null)).operatingSystem)
          | extend ASN     = tostring(column_ifexists("AutonomousSystemNumber", ""))
          | summarize KnownCountries = make_set(Country, 50),
                      KnownASNs      = make_set(ASN, 50),
                      KnownOS        = make_set(OS, 25)
              by UserPrincipalName;
      SigninLogs
      | where TimeGenerated > ago(recent)
      | where UserPrincipalName =~ "{{upn}}"
      | where ResultType == "0"
      | extend Country = tostring(column_ifexists("LocationDetails", dynamic(null)).countryOrRegion)
      | extend OS      = tostring(column_ifexists("DeviceDetail", dynamic(null)).operatingSystem)
      | extend ASN     = tostring(column_ifexists("AutonomousSystemNumber", ""))
      | extend Managed = tostring(column_ifexists("DeviceDetail", dynamic(null)).isManaged)
      | join kind=inner baseline on UserPrincipalName
      | extend Score =
            toint(not(set_has_element(KnownCountries, Country))) * 3
          + toint(not(set_has_element(KnownASNs, ASN)))          * 3
          + toint(not(set_has_element(KnownOS, OS)))             * 1
          - toint(Managed =~ "true")                             * 2
      | where Score >= 4
      | project TimeGenerated, UserPrincipalName, IPAddress, Country, ASN, OS, Managed, Score, AppDisplayName
      | order by Score desc
      //
      // Look for: Score >= 4 with Managed != true — new country AND new ASN on
      //   an unmanaged device is the strongest single signal short of a token
      //   replay hit in step 6.
      // Benign twin: a brand-new account, or a user with < 14 days of history,
      //   has an empty baseline and will score high on everything. Check
      //   FirstSeen before believing it.
      // Next: regardless of score, continue to step 3 — persistence is
      //   established in minutes, so absence of novelty does not clear the account.

  - id: inbox-rules
    title: 3. Persistence — mailbox rules created after the sign-in
    kind: scenario
    param: upn
    dialect: log_analytics
    tables: [OfficeActivity]
    kql: |
      // PURPOSE: the single most common post-compromise action on a mailbox.
      // NOTE: Exchange operations live in OfficeActivity, NOT AuditLogs.
      OfficeActivity
      | where TimeGenerated > ago(7d)
      | where OfficeWorkload =~ "Exchange"
      | where Operation in~ ("New-InboxRule", "Set-InboxRule", "UpdateInboxRules",
                             "Set-Mailbox", "Set-TransportRule", "New-TransportRule")
      | extend TargetUser = tostring(column_ifexists("UserId", ""))
      | where TargetUser =~ "{{upn}}"
          or tostring(column_ifexists("MailboxOwnerUPN", "")) =~ "{{upn}}"
      | extend RuleParams = tostring(column_ifexists("Parameters", ""))
      | project TimeGenerated, Operation, TargetUser,
                ClientIP  = tostring(column_ifexists("ClientIP", "")),
                ResultStatus = tostring(column_ifexists("ResultStatus", "")),
                RuleParams
      | order by TimeGenerated desc
      //
      // Look for: a rule whose parameters contain ForwardTo, ForwardAsAttachmentTo,
      //   RedirectTo, or DeleteMessage — especially forwarding to an EXTERNAL
      //   domain, or a rule that files security/IT mail into RSS/Archive/Deleted
      //   (the attacker hiding the "unusual sign-in" notice from the user).
      // Also: compare ClientIP here to the IP from step 1. A rule created from
      //   the same IP is confirmation, not coincidence.
      // Next: any hit here = confirmed compromise. Preserve the rule (screenshot
      //   + export), then remove it, revoke sessions, and reset credentials.
      //   Continue to steps 4-6 — attackers commonly set more than one anchor.

  - id: oauth-consent
    title: 4. Persistence — OAuth application consent granted by this account
    kind: scenario
    param: upn
    dialect: log_analytics
    tables: [AuditLogs]
    kql: |
      // PURPOSE: an illicit consent grant survives a password reset and MFA
      // re-registration. If you only reset the password, you have not evicted them.
      AuditLogs
      | where TimeGenerated > ago(7d)
      | where OperationName has_any ("Consent to application",
                                     "Add OAuth2PermissionGrant",
                                     "Add app role assignment grant to user",
                                     "Add delegated permission grant",
                                     "Add service principal")
      | extend Actor = tostring(column_ifexists("InitiatedBy", dynamic(null)).user.userPrincipalName)
      | extend ActorIp = tostring(column_ifexists("InitiatedBy", dynamic(null)).user.ipAddress)
      | where Actor =~ "{{upn}}"
      | extend Target = tostring(column_ifexists("TargetResources", dynamic(null)))
      | project TimeGenerated, OperationName, Actor, ActorIp,
                Result = tostring(column_ifexists("Result", "")),
                Target
      | order by TimeGenerated desc
      //
      // Look for: any consent granted in the same window as the step-1 sign-in,
      //   particularly to an application you do not recognise, or one requesting
      //   Mail.Read / Mail.ReadWrite / offline_access / Files.Read.All.
      // Benign twin: users legitimately consent to SaaS tools. Check whether the
      //   ActorIp matches the suspicious IP from step 1 before calling it malicious.
      // Next: a hit means credential reset alone is insufficient — revoke the
      //   service principal / OAuth grant and audit what it accessed.

  - id: auth-method-registration
    title: 5. Persistence — new device or MFA method registered
    kind: scenario
    param: upn
    dialect: log_analytics
    tables: [AuditLogs]
    kql: |
      // PURPOSE: registering their own authenticator or device is how an attacker
      // keeps access through your password reset. Check this BEFORE you reset.
      AuditLogs
      | where TimeGenerated > ago(7d)
      | where OperationName has_any ("Add security info", "User registered security info",
                                     "User changed default security info",
                                     "Add registered device", "Register device",
                                     "Add device", "Update device",
                                     "Admin registered security info",
                                     "User deleted security info")
      | extend Actor   = tostring(column_ifexists("InitiatedBy", dynamic(null)).user.userPrincipalName)
      | extend ActorIp = tostring(column_ifexists("InitiatedBy", dynamic(null)).user.ipAddress)
      | extend TargetUpn = tostring(column_ifexists("TargetResources", dynamic(null)))
      | where Actor =~ "{{upn}}" or TargetUpn has "{{upn}}"
      | project TimeGenerated, OperationName, Actor, ActorIp,
                Result = tostring(column_ifexists("Result", "")),
                TargetUpn
      | order by TimeGenerated desc
      //
      // Look for: a security-info / device registration within minutes of the
      //   step-1 sign-in, from the same ActorIp. That is the attacker provisioning
      //   their own MFA so the account survives your reset.
      // Benign twin: a genuine new-phone enrolment — corroborate with the user.
      // Next: a hit changes remediation ORDER — remove the attacker-registered
      //   method and revoke refresh tokens FIRST, then reset the credential.
      //   Resetting first simply hands them a fresh session.

  - id: token-replay
    title: 6. Token replay — one session seen from multiple IPs
    kind: scenario
    param: upn
    dialect: log_analytics
    tables: [SigninLogs]
    kql: |
      // PURPOSE: covers the case where nothing above fires because the credential
      // was never stolen — a session token was. A single session id appearing from
      // two networks minutes apart is theft, not travel.
      SigninLogs
      | where TimeGenerated > ago(7d)
      | where UserPrincipalName =~ "{{upn}}"
      | where ResultType == "0"
      | extend SessionKey = coalesce(tostring(column_ifexists("SessionId", "")),
                                     tostring(column_ifexists("UniqueTokenIdentifier", "")))
      | where isnotempty(SessionKey)
      | extend Country = tostring(column_ifexists("LocationDetails", dynamic(null)).countryOrRegion)
      | extend ASN     = tostring(column_ifexists("AutonomousSystemNumber", ""))
      | summarize IPs        = dcount(IPAddress),
                  IPList     = make_set(IPAddress, 25),
                  ASNs       = make_set(ASN, 25),
                  Countries  = make_set(Country, 25),
                  Apps       = make_set(AppDisplayName, 25),
                  FirstSeen  = min(TimeGenerated),
                  LastSeen   = max(TimeGenerated)
          by SessionKey, UserPrincipalName
      | where IPs > 1
      | extend SpanMinutes = datetime_diff('minute', LastSeen, FirstSeen)
      | project UserPrincipalName, SessionKey, IPs, SpanMinutes, IPList, ASNs, Countries, Apps, FirstSeen, LastSeen
      | order by IPs desc, SpanMinutes asc
      //
      // Look for: IPs > 1 with a SHORT SpanMinutes and different ASNs/Countries —
      //   the same token used from two networks at once. Include the step-1 IP in
      //   IPList and it is confirmed replay.
      // Benign twin: a long SpanMinutes across one ASN is usually a roaming client
      //   or a proxy hop, not theft. Mobile carriers legitimately change egress IP.
      // Next: replay means the credential reset is NOT the fix — revoke refresh
      //   tokens / sign-in sessions for the account, then review what the session
      //   accessed (steps 3-5 outputs) before closing.
